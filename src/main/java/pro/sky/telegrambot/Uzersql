-- Создайте пользователя
CREATE USER TelegramBot WITH PASSWORD 'karl';

-- Создание таблицы машин
CREATE TABLE car (
    id BIGSERIAL PRIMARY KEY,
    brand VARCHAR(100) NOT NULL,
    model VARCHAR(100) NOT NULL,
    price NUMERIC(15,2) NOT NULL

    -- Если пользователя нет, создайте его
    CREATE USER karl WITH PASSWORD 'новый_пароль';

    -- Если пользователь есть, но пароль неверный, смените пароль
    ALTER USER karl WITH PASSWORD 'новый_пароль';

    -- Дайте пользователю права на базу данных
    ALTER USER karl CREATEDB;